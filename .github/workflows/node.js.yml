name: Playwright-Cucumber CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

  # ------------------------------------------------------
  # 1️⃣ JOB: RUN TESTS + GENERATE DOCX REPORTS
  # ------------------------------------------------------
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔧 Set Up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🐍 Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 Install Python Dependencies
        run: |
          pip install python-docx pillow

      - name: 📦 Install Node Dependencies
        run: npm install --no-fund --no-audit

      - name: 🎭 Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: 🛠 Run Smoke Tests
        run: |
          mkdir -p test-output
          npx cucumber-js -p all --tags "@smoke1" || echo "❌ Tests failed"

      # ------------------------------------------------------
      # Upload the two DOCX reports generated in test-output/
      # ------------------------------------------------------
      - name: 📤 Upload Word Report Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: |
            test-output/Automation_Step_Report.docx
            test-output/Automation_Screenshot_Report.docx
          if-no-files-found: warn


  # ------------------------------------------------------
  # 2️⃣ JOB: SEND EMAIL WITH ATTACHMENTS
  # ------------------------------------------------------
  send_mail:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repo
        uses: actions/checkout@v4

      - name: 📥 Download Report Artifacts
        uses: actions/download-artifact@v4
        with:
          name: test-output        
          path: ./test-output

      - name: 📦 Install Node Dependencies
        run: npm install

      - name: 🛠 Compile TypeScript
        run: npx tsc

      - name: ▶️ Run Email Sender Script
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        run: node utils/emailSender.js
